pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh label: '', script: '''#!/bin/bash
                    echo "hello world" 
                '''
            }
        }
    }
}
